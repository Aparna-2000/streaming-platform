# Generate a long random string (>= 64 chars)
JWT_ACCESS_SECRET=change_me_to_a_long_random_string
# Optional: issuer/audience metadata
JWT_ISSUER=streaming-platform-api
JWT_AUDIENCE=streaming-platform-web
# Default access token lifetime (kept here for visibility; code sets 15m)
JWT_ACCESS_EXPIRES=15m
